# Ядро участника 1 работает без API-ключей.
# API читает DATASET_PATH из системного окружения; .env автоматически не загружается.
# DATASET_PATH=data/hackathon-dataset-anonymized.jsonl

# Резерв для модуля участника 2. В текущем бэкенде эти переменные не используются.
LLM_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=
NVIDIA_API_KEY=
NVIDIA_MODEL=
