# HackAlem — бэкенд участника 1

Готовая часть проекта «Умный подбор подрядчиков (#79-lite)»: чтение данных, строгие фильтры, воспроизводимое ранжирование, три исхода, диагностика исключений и контракт для LLM/Streamlit.

Реализовано по приложенному PDF и исходному CSV из 66 профилей. Google Docs по переданной ссылке оказался недоступен; дополнительные требования из него не проверены. Исходный HTML использован для сверки описания календаря. Данные не расширялись и не исправлялись вручную.

## Быстрый запуск без установки библиотек

Нужен Python 3.10 или новее. Проверено на Python 3.12. Распакуйте архив и откройте терминал **в папке с `main.py`**.

Windows:

```powershell
py demo.py
py main.py validate
py main.py search --request examples/01_dense_request.json
```

macOS/Linux: замените `py` на `python3`.

`demo.py` показывает шесть готовых запросов, включая три обязательных сценария и отдельные случаи B/C. Для демонстрации своего бэкенда API-ключи и интернет не нужны. Это демонстрация участника 1; нейросетевые объяснения добавляет участник 2.

## HTTP API и страница проверки запросов

Windows, PowerShell или cmd, без активации окружения:

```powershell
py -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m uvicorn api:app --reload --host 127.0.0.1 --port 8000
```

Откройте http://127.0.0.1:8000/docs. В разделе `POST /recommendations` нажмите **Try it out**, вставьте JSON ниже и нажмите **Execute**. Это страница тестирования API; пользовательский интерфейс Streamlit делает участник 3.

```json
{
  "city": "Алматы",
  "date": "2026-10-10",
  "event_format": "корпоратив",
  "category": "Ведущий",
  "budget_kzt": 1500000,
  "languages": [],
  "duration_hours": null
}
```

macOS/Linux:

```bash
python3 -m venv .venv
.venv/bin/python -m pip install -r requirements.txt
.venv/bin/python -m uvicorn api:app --reload --host 127.0.0.1 --port 8000
```

API загружает датасет один раз при старте. `DATASET_PATH` в системном окружении позволяет выбрать другой CSV/JSONL. Относительный путь считается от рабочей папки процесса; путь по умолчанию — от расположения проекта. `.env` автоматически не загружается.

| Метод и путь | Назначение |
|---|---|
| `GET /health` | Готовность, число профилей и версия данных |
| `GET /options` | Города, категории, форматы, языки и диапазон дат для UI |
| `POST /recommendations` | Полный результат фильтрации и контекст для LLM |
| `GET /docs` | Интерактивная документация FastAPI |
| `GET /openapi.json` | Схема запросов API |

## Структура файлов и ответственность

| Файл | Что делает |
|---|---|
| `src/models.py` | Поля профиля и запроса, проверка типов и дат |
| `src/data_loader.py` | Чтение CSV/JSONL, проверка записей, конвертация |
| `src/filter_engine.py` | Hard filters, ранжирование, счётчики, A/B/C, до 3 карточек |
| `src/llm_contract.py` | Сообщения для LLM и присоединение объяснений без смены ID/порядка |
| `api.py` | HTTP API FastAPI для команды |
| `main.py` | Команды `search`, `validate`, `options`, `convert` |
| `demo.py` | Шесть готовых сценариев |
| `data/` | Исходный CSV и равнозначный JSONL |
| `examples/` | Запросы и полный пример ответа |
| `tests/` | Тесты данных, фильтров, HTTP API и контракта LLM |
| `docs/TEAM_INTEGRATION.md` | Как подключить существующий `src/llm_explainer.py` и будущий UI |
| `docs/DEFENSE.md` | Короткое объяснение для защиты |
| `docs/VALIDATION.md` | Фактические результаты проверки и скорость |
| `docs/demo-results.json` | Ожидаемые результаты демосценариев |

## Как работает пайплайн

CSV/JSONL читается в неизменяемые профили. Запрос проверяется до отбора. Затем выбираются точные город и категория, проверяются дата, бюджет, формат, языки и длительность. Прошедшие профили сортируются по `(price_from_kzt, id)` по возрастанию. Первые 5 попадают в `candidates`, первые 3 — в `cards` и `llm_context.selected_ids`.

Одинаковые данные и запрос дают одинаковый полный результат. В ответе нет случайного выбора, текущего времени или нестабильных метрик. Изменение порядка строк CSV не меняет результат. Хеш `dataset_version` рассчитывается по нормализованным данным, включая описания и календари.

Цена используется как прозрачное правило сортировки, а не оценка качества. Из дешёвой или дорогой цены не делаются выводы о профессионализме. Языки при их указании — обязательный фильтр, поэтому не используются повторно как формальный балл ранжирования.

## Точные правила

| Условие | Правило |
|---|---|
| Город | Точное равенство `city` после удаления пробелов по краям |
| Категория | Точный элемент `categories`, не поиск подстроки |
| Дата | Даты нет в `busy_dates` |
| Бюджет | `price_from_kzt <= budget_kzt`, граница включается |
| Формат | Точный элемент `event_formats` |
| Языки | Все переданные `languages` присутствуют в профиле; `[]` отключает фильтр |
| Длительность | `duration_hours <= max_hours`, если указаны оба значения |
| `max_hours=null` | По PDF услуга не привязана к присутствию; не исключаем по длительности |
| Дата вне окна | Ошибка ввода, а не утверждение о свободной дате |
| Флаги данных | `synthetic`, `city_imputed`, `price_imputed` сохраняются в кандидатах и карточках |

Календарь известен только для **2026-09-23 — 2026-12-31 включительно**. Значения «Астана» и «Алматы» не заменяются друг другом. Регистр не исправляется: для UI берите значения из `/options`.

Начальная цена не равна гарантированной итоговой стоимости. `synthetic=false` означает исходный анонимизированный профиль, а не проверку реальности личности. Для профилей с дополненной ценой или городом возвращаются соответствующие флаги и примечания.

Повреждённые записи не пропускаются молча: загрузчик сообщает файл, строку и причину. Дубликаты ID, неверные даты, отрицательные цены, строковые JSON-флаги и неправильные списки отклоняются. В CSV списки разделены `|`, `True/False` явно преобразуются в bool, пустые часы — в `null`.

## Три исхода и причины

| `status` | `outcome` | Смысл |
|---|---|---|
| `ok` | A | Есть подходящие профили |
| `no_category_in_city` | B | В датасете нет этой категории в этом городе |
| `no_matches` | C | Категория в городе есть, но никто не проходит все условия |

Все три бизнес-исхода возвращают HTTP 200. Неверные параметры возвращают HTTP 422. CLI для ошибки ввода/данных завершает работу с кодом 2.

В `excluded` есть ID и все причины исключения каждого профиля из пула «город + категория». В `summary.exclusions_by_reason` — соответствующие счётчики. Они **пересекаются**: один человек может одновременно быть занят и дороже бюджета. Число уникальных исключённых профилей находится в `excluded_profiles`. Город и категория применяются до этих счётчиков.

Если найдено меньше трёх, выдаётся фактическое количество. Сообщение указывает исходный размер категории в городе, остаток и причины исключения. Недостающие карточки не заполняются неподходящими профилями.

## Подключение к app.py напрямую

```python
from src import FilterEngine, load_profiles

engine = FilterEngine(load_profiles())  # один раз при запуске
result = engine.search({
    "city": "Алматы",
    "date": "2026-10-10",
    "event_format": "корпоратив",
    "category": "Ведущий",
    "budget_kzt": 1500000,
    "languages": [],
    "duration_hours": None,
})
print(result["message"])
print(result["cards"])
```

Альтернативный простой интерфейс: `find_contractors(profiles, user_request)`. При повторных запросах предпочтителен один `FilterEngine`, чтобы не перечитывать и не хешировать данные каждый раз.

## OpenAI API, затем NVIDIA API

В текущей части **реализован контракт передачи данных**, а сетевой вызов LLM остаётся в уже существующем модуле `src/llm_explainer.py` участника 2. Рабочие ключи не требуются и не включены. `build_llm_messages(result)` готовит сообщения для Chat Completions; `apply_explanations(result, texts)` принимает словарь `id -> текст`, проверяет ID и сохраняет порядок карточек. Пример подключения существующего модуля приведён в `docs/TEAM_INTEGRATION.md`.

Существующий модуль участника 2 использует SDK `openai` и умеет выбирать OpenAI или NVIDIA. Для внешнего вызова дополнительно установите `pip install openai` и настройте ключ выбранного провайдера в системном окружении. NVIDIA использует `base_url="https://integrate.api.nvidia.com/v1"`, отдельный ключ и идентификатор модели из своего каталога. Параметры модели и поддержку JSON-ответов нужно проверять отдельно. `.env.example` содержит только заготовку названий переменных; бэкенд автоматически `.env` не загружает.

Отбор кандидатов всегда остаётся за Python. При ошибке, таймауте или неверных ID ответ LLM отбрасывается и сохраняются фактические шаблонные подписи. `apply_explanations` проверяет форму и ID, но не доказывает истинность свободного текста: проверка фактов и качества объяснений — часть LLM-модуля. Пустая выдача уже объяснена бэкендом и не требует обращения к модели.

Ориентир 10 секунд относится к полному приложению. Для участника 2 нужен общий срок ожидания, ограничение повторов и резервный текст. `temperature=0` не гарантирует одинаковые формулировки; стабильность порядка обеспечивается здесь, сохранение одинаковых текстов при необходимости — кешем по запросу, версии данных, модели и промпту.

Документация провайдеров:
- https://developers.openai.com/api/docs/quickstart
- https://developers.openai.com/api/reference/python
- https://docs.api.nvidia.com/nim/docs/api-quickstart
- https://developers.openai.com/cookbook/examples/reproducible_outputs_with_the_seed_parameter

## Демосценарии

```powershell
py demo.py
```

| Файл запроса | Сценарий | Ожидается |
|---|---|---|
| `01_dense_request.json` | Алматы, ведущий, корпоратив, 10 октября, 1 500 000 ₸ | 5 кандидатов, 3 карточки |
| `02_rare_request.json` | Алматы, флорист, те же дата/формат/бюджет | 1 карточка и пояснение |
| `03_no_matches_request.json` | Алматы, ведущий, 26 декабря, 50 000 ₸ | C: ни одного совпадения |
| `04_no_category_request.json` | Астана, декоратор | B: нет категории в городе |
| `05_other_date_request.json` | Как сценарий 1, но 11 октября | 4 кандидата, другой состав карточек |
| `06_languages_duration_request.json` | Ведущий, русский + английский, 4 часа | 1 карточка |

Для сохранения полного ответа в файл:

```powershell
py main.py search --request examples/01_dense_request.json --output output/result.json
```

## Тесты

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements-dev.txt
.\.venv\Scripts\python.exe -m pytest -q
```

Тесты покрывают все фильтры, границы бюджета/длительности, null-часы, несколько языков, занятость площадок, три исхода, пересекающиеся причины, повторяемость, лимит 5/3, изменение даты, преобразование CSV/JSONL, ошибочные данные, HTTP 422, CLI и присоединение LLM-текстов. Дополнительно 200 фиксированных случайных запросов сверяются с независимым прямым отбором по исходным профилям.

## Соответствие критериям жюри

| Критерий | Что готово в части участника 1 |
|---|---|
| Работоспособность, 25 баллов | Все фильтры, A/B/C, до 3 карточек, 6 демосценариев |
| Техническая реализация, 25 | Отдельное ядро, API, строгая загрузка, контракт LLM и тесты |
| README и воспроизводимость, 25 | Датасет в `data/`, зависимости, команды и ожидаемые ответы |
| Ценность, 15 | Понятные причины пустой выдачи, отсутствие неподходящих подстановок |
| Развитие, 10 | Возможность подключения OpenAI/NVIDIA и интерфейса без изменения фильтров |

Это описание реализованной части, а не обещание баллов. Финальная оценка команды также зависит от LLM-объяснений и демонстрации объединённого приложения.
